#!/usr/bin/env bash

compile() {
  outfile=$(basename $1)
  clang -Wextra -ansi -pedantic "$1.c" -o "./bin/$outfile" -O2 "${@:2}"
}

llvm() {
  clang -S -Wextra -ansi -pedantic "$1.c" -o "./$1.ll" -emit-llvm "${@:2}"
}

asm() {
  clang -S -Wextra -ansi -pedantic "$1.c" -o "./$1.s" "${@:2}"
}
